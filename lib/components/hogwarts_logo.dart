import 'package:flutter/material.dart';

class HogwartsLogo extends StatelessWidget {
  const HogwartsLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      "ReuryPotter",
      style: TextStyle(fontFamily: "HarryPotter", fontSize: 96),
    );
  }
}
